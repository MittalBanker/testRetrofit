package com.example.digicorp.testretrofit.API;
//package com.example.digicorp.testretrofit.model.gitmodel;

import retrofit.http.GET;
import retrofit.http.Path;

/**
 * Created by digicorp on 08/06/16.
 */
interface gitapi {
   // @GET("/users/{user}")      //here is the other url part.best way is to start using /
    //public void getFeed(retrofit.http.Path("user") String user,Callback<gitmodel> response);     //string user is for passing values from edittext for eg: user=basil2style,google
// /   //resp//onse is the response from the server which is now in the POJO
}
